# TradingAgents 交易仪表盘用户手册

## 📊 概述

TradingAgents 交易仪表盘是一个现代化的数据密集型分析平台，专为A股交易员设计。它提供实时行情、技术分析、资金流向等多维度的交易决策支持。

### 设计系统
- **风格**: Data-Dense Dashboard (数据密集型仪表盘)
- **色彩方案**: 专业蓝色 (#1E40AF) + 琥珀色CTA (#F59E0B)
- **字体**: Fira Code (数据) + Fira Sans (界面)
- **性能**: ⚡ 优秀 | ♿ WCAG AA 无障碍认证

---

## 🚀 快速开始

### 1. 启动仪表盘

#### 方法A: 直接打开HTML（推荐用于初期测试）
```bash
# Windows
start TradingAgent\trading_dashboard.html

# macOS
open TradingAgent/trading_dashboard.html

# Linux
xdg-open TradingAgent/trading_dashboard.html
```

#### 方法B: 使用Python本地服务器
```bash
cd TradingAgent
python -m http.server 8000
# 访问: http://localhost:8000/trading_dashboard.html
```

### 2. 测试后端服务
```bash
cd TradingAgent
python trading_dashboard_backend.py
```

输出示例：
```
================================================================================
交易仪表盘后端服务测试
================================================================================

【KPI指标】
  涨跌家数: 2,847 (↑ 12.5%)
  成交总额: ¥825.4B (↑ 8.3%)
  平均涨幅: +2.34% (↑ 强势)
  主力资金: ¥12.3B (↑ 净流入)

【大盘指数】
  上证指数: 3564.20 (+2.45%)
  深证成指: 10825.47 (+1.87%)
  创业板指: 2134.56 (+2.50%)

... 更多数据 ...

✅ 测试完成！
```

---

## 📈 仪表盘功能详解

### 1. KPI 指标卡片
位于页面顶部，展示4个关键绩效指标：

| 指标 | 说明 | 用途 |
|------|------|------|
| 涨跌家数 | 上涨股票数量 | 判断市场热度 |
| 成交总额 | 市场成交金额 | 评估流动性 |
| 平均涨幅 | 市场平均涨幅 | 判断市场强弱 |
| 主力资金 | 大资金净流入 | 判断主力动向 |

**交互**: 鼠标悬停显示数据tooltip，点击可查看详细分析

### 2. 筛选条件栏
```
┌─────────────┬─────────────┬─────────────────┐
│ 股票类型    │ 涨跌排序    │ 最小涨幅       │
├─────────────┼─────────────┼─────────────────┤
│ ▼ 全部      │ ▼ 涨幅排序  │ 输入框 _______ │
│  主板       │  成交量     │                 │
│  创业板     │  价格       │ [应用筛选] [导出]│
│  科创板     │             │                 │
└─────────────┴─────────────┴─────────────────┘
```

**使用示例**:
- 仅看创业板涨幅>3%的股票: 选择"创业板" + 输入"3" + 点击"应用筛选"
- 按成交量排序: 选择"成交量" + 点击"应用筛选"

### 3. 大盘走势图
- **类型**: 多线柱形图
- **包含**: 上证指数、深证成指、创业板指
- **时间**: 09:30 - 15:00（5分钟数据）
- **交互**: 鼠标悬停显示具体数值，可缩放

### 4. 板块热力分析
- **类型**: 水平条形图
- **排序**: 按涨幅降序
- **颜色**: 绿色(高涨幅) → 黄色(中等) → 红色(下跌)
- **用途**: 快速识别热点板块

### 5. 股票排行榜
数据密集型表格，包含50只股票：

```
排名 代码    名称        现价    涨幅%  成交量  市值   PE   操作
──────────────────────────────────────────────────────────────
1   600519  贵州茅台  2850.50  ↑5.23%  1.2M  2.4T  85.5  [分析]
2   000858  五粮液     298.75  ↑4.85%  3.5M  1.2T  62.3  [分析]
3   000651  格力电器    45.20  ↓2.15%  5.8M  980B  28.5  [分析]
...
```

**交互**:
- 点击行: 突出显示该股票
- 点击[分析]: 打开详细分析页面
- 滚动: 加载更多股票
- 列头排序: 按该列排序

### 6. 技术指标分析

#### MACD 指标分布
```
强烈看多  看多     平衡     看空     强烈看空
  ████    ████████  █████████████  █████  ███
  285     680      1240           520     180
```

#### RSI 强弱指数
- **超买区 (>70)**: 340只股票 - 风险警示
- **正常区**: 2180只股票 - 运行正常
- **超卖区 (<30)**: 276只股票 - 底部机会

### 7. 资金流向分析
```
净流入 (亿元)  ┌──────────────────┐
            │ 周一  周二  周三  周四  周五  今日
            │ 185  225  180  240  205  230
            │
净流出 (亿元)│  45   35   60   32   48   38
            └──────────────────┘
```

**关键指标**:
- 净流入: ¥523.2B (7日累计)
- 净流出: ¥187.5B (7日累计)
- 日均成交: ¥825.4B

---

## 🎨 UI/UX 设计详解

### 色彩方案
```
┌─────────────────────────────────────────────┐
│ 色彩      │ 十六进制  │ 用途                │
├─────────────────────────────────────────────┤
│ 主色      │ #1E40AF  │ 标题、图表线        │
│ 次色      │ #3B82F6  │ 交互元素、边框      │
│ CTA       │ #F59E0B  │ 关键操作按钮        │
│ 背景      │ #F8FAFC  │ 页面背景            │
│ 文本      │ #1E3A8A  │ 主文本              │
│ 上升      │ #10B981  │ 涨幅、买入信号      │
│ 下降      │ #EF4444  │ 跌幅、卖出信号      │
└─────────────────────────────────────────────┘
```

### 响应式设计
```
┌────────────────────────────────────────────────┐
│ 分辨率        │ 布局                            │
├────────────────────────────────────────────────┤
│ 1440px+       │ 2列仪表板 + 完整筛选栏         │
│ 1024px        │ 单列仪表板 + 简化筛选          │
│ 768px(平板)   │ 单列布局 + 栈式导航            │
│ 375px(手机)   │ 垂直布局 + 可折叠菜单          │
└────────────────────────────────────────────────┘
```

### 无障碍 (A11y) 支持
- ✅ WCAG AA 认证
- ✅ 键盘导航: Tab键切换焦点
- ✅ 屏幕阅读器: 完整的ARIA标签
- ✅ 颜色对比度: 4.5:1最小对比度
- ✅ 深色模式: 自动适配系统设置
- ✅ 减速动画: 尊重 `prefers-reduced-motion`

---

## 💻 后端服务 API

### 文件位置
```
TradingAgent/trading_dashboard_backend.py
```

### 核心类

#### TradingDashboardService
主要数据服务类，包含所有数据获取逻辑

```python
service = get_service()

# 获取KPI数据
kpis = service.get_kpi_data()

# 获取大盘指数
indices = service.get_market_indices()

# 获取涨幅排行
stocks = service.get_top_stocks(
    limit=50,           # 返回数量
    stock_type="all",   # all/mainboard/cyb/kcb
    sort_by="change",   # change/volume/price
    min_change=0        # 最小涨幅
)

# 获取板块分析
sectors = service.get_sector_analysis()

# 获取技术指标
technical = service.get_technical_analysis()

# 获取资金流向
money_flow = service.get_money_flow()

# 分析单个股票
analysis = service.analyze_stock("600519")

# 导出数据
csv_data = service.export_data(export_type="csv")
```

#### DashboardAPI
便捷API接口

```python
from trading_dashboard_backend import get_dashboard_data

# 统一调用接口
result = get_dashboard_data("get_kpi")
result = get_dashboard_data("get_stocks", limit=50, sort_by="change")
result = get_dashboard_data("analyze_stock", code="600519")
result = get_dashboard_data("export_data", type="csv")
```

### 集成到GUI
如果要将仪表盘集成到现有的 `a_share_gui_compatible.py` 中：

```python
from trading_dashboard_backend import get_dashboard_data, TradingDashboardService

# 在GUI类中添加方法
def show_trading_dashboard(self):
    """打开交易仪表盘"""
    # 方式1: 在文本框中显示JSON
    summary = get_dashboard_data("get_summary")
    self.output_text.insert('end', json.dumps(summary['data'], 
                                              ensure_ascii=False, indent=2))
    
    # 方式2: 使用默认浏览器打开HTML
    import webbrowser
    webbrowser.open('file://' + os.path.abspath('trading_dashboard.html'))
```

---

## 🔄 实时数据更新

### 当前实现 (示例数据)
- 每3秒更新一次KPI指标
- 图表自动刷新
- 表格行高亮交互

### 升级为实时数据
替换 `trading_dashboard_backend.py` 中的示例数据：

```python
def get_top_stocks(self, ...):
    """改为调用真实API"""
    # 示例: 调用 a_share_gui_compatible.py 中的分析函数
    from a_share_gui_compatible import AShareAnalyzerGUI
    
    analyzer = AShareAnalyzerGUI(None)
    real_stocks = analyzer.get_cached_stock_codes()
    
    # 处理数据...
    return processed_data
```

---

## 🛠️ 开发指南

### 修改样式
编辑 `trading_dashboard.html` 中的 `<style>` 标签：

```css
/* 示例: 改变主色 */
.widget-title {
    color: #1e40af;  /* 改为你的颜色 */
}

/* 示例: 调整响应式断点 */
@media (max-width: 1024px) {
    .dashboard-grid {
        grid-template-columns: 1fr;  /* 改为1列 */
    }
}
```

### 添加新图表
使用 Chart.js 库：

```javascript
// 在容器中添加 <canvas id="myChart"></canvas>
const ctx = document.getElementById('myChart').getContext('2d');
new Chart(ctx, {
    type: 'line',  // 或 bar, doughnut, etc
    data: {
        labels: ['Jan', 'Feb', 'Mar'],
        datasets: [{
            label: '数据',
            data: [10, 20, 15],
            borderColor: '#1e40af',
            backgroundColor: 'rgba(30, 64, 175, 0.1)'
        }]
    },
    options: {
        responsive: true,
        maintainAspectRatio: false
    }
});
```

### 添加新API端点
在 `trading_dashboard_backend.py` 中：

```python
class TradingDashboardService:
    def get_my_new_data(self):
        """新增数据方法"""
        return {...}

class DashboardAPI:
    def handle_request(self, request_type, **kwargs):
        handlers = {
            ...
            "get_my_new_data": self.service.get_my_new_data,
            ...
        }
```

---

## 📱 使用场景

### 场景1: 快速选股
1. 打开仪表盘
2. 在"股票类型"选择"主板"
3. 在"最小涨幅"输入"3"
4. 点击"应用筛选"
5. 查看涨幅>3%的主板股票排行

### 场景2: 板块轮动
1. 查看"板块热力分析"
2. 识别涨幅最高的板块 (如计算机)
3. 点击该板块的代表股票[分析]按钮
4. 获取详细分析建议

### 场景3: 资金流向分析
1. 查看"资金流向分析"图表
2. 观察7日趋势
3. 对比"主力资金"KPI
4. 确认是否为主力建仓阶段

### 场景4: 技术面风险评估
1. 查看"MACD指标分布"
2. 查看"RSI强弱指数"
3. 如果超买(>70)过多，需谨慎追高
4. 如果超卖(<30)较多，考虑底部机会

---

## ⚡ 性能优化

### 已实现
- ✅ CSS Grid 布局优化
- ✅ SVG 图标替代emoji
- ✅ 图表虚拟化渲染
- ✅ 懒加载表格数据

### 可进一步优化
- [ ] WebSocket 实时推送
- [ ] IndexedDB 本地缓存
- [ ] Service Worker 离线支持
- [ ] 代码分割和动态导入

---

## 🐛 常见问题

**Q: 图表不显示?**
A: 确保 Chart.js 库已加载: 检查浏览器控制台是否有加载错误

**Q: 数据不更新?**
A: 当前使用静态示例数据。要启用实时数据，需集成真实API

**Q: 手机上布局混乱?**
A: 在浏览器开发者工具中检查 viewport 设置，确保有 `<meta name="viewport">`

**Q: 深色模式显示不正确?**
A: 检查 `@media (prefers-color-scheme: dark)` 中的颜色定义

---

## 📞 支持

- 📧 问题报告: issues@tradingagents.local
- 💬 讨论区: discussions@tradingagents.local
- 📖 文档: [README.md](README.md)

---

## 📄 文件清单

```
TradingAgent/
├── trading_dashboard.html           # 前端仪表盘 (主要界面)
├── trading_dashboard_backend.py     # 后端数据服务
├── TRADING_DASHBOARD_README.md      # 本文件
└── design-system/tradingagents/     # 设计系统文档
    ├── MASTER.md                    # 全局设计指南
    └── pages/                       # 页面特定覆盖
```

---

**最后更新**: 2026-01-26  
**版本**: 1.0.0  
**许可证**: MIT
